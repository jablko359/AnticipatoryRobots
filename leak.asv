classdef leak
    properties
        x   %position
        t   %time of activation
        i   %intensity
        tau %time of activity
        theta %time when leak becomes an obstacle
        rho %damages thah leak causes to passing robots       
    end
    
    methods
        function obst = createObstacle(obj,obstacles,corridorLength)
            obst = obstacle;
            obst.a = 1;
            obst.x = obj.x;
            obst.L = obj.i 
        end
    end
end